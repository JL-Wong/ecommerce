import 'package:flutter/material.dart';

class Completeorderpage extends StatelessWidget {
  const Completeorderpage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is Complete order page'),
    );
  }
}